---
services:
  postgres:
    restart: unless-stopped
    image:
      postgres:16
    environment:
      - POSTGRES_DB=task_tracker
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=qwerty
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5

  migrations:
    # One-shot job: run migrations and exit 0. Must NOT auto-restart, otherwise
    # it would loop forever and never satisfy service_completed_successfully.
    restart: "no"
    build:
      context: .
      dockerfile: common/Dockerfile
    environment:
      - DATABASE_NAME=task_tracker
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=qwerty
      - DATABASE_HOST=postgres
    depends_on: { postgres: { condition: service_healthy } }

  tasks:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/tasks/Dockerfile
    environment:
      - DB_NAME=task_tracker
      - DB_HOST=postgres
      - AMQP_URL=amqp://guest:guest@rabbitmq/
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully

  users:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/users/Dockerfile
    environment:
      - DB_NAME=task_tracker
      - DB_HOST=postgres
      - AMQP_URL=amqp://guest:guest@rabbitmq/
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully

  comments:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/comments/Dockerfile
    environment:
      - DB_NAME=task_tracker
      - DB_HOST=postgres
      - AMQP_URL=amqp://guest:guest@rabbitmq/
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully

  tags:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/tags/Dockerfile
    environment:
      - DB_NAME=task_tracker
      - DB_HOST=postgres
      - AMQP_URL=amqp://guest:guest@rabbitmq/
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully

  attachments:
    restart: unless-stopped
    build:
      context: .
      dockerfile: services/attachments/Dockerfile
    environment:
      - DB_NAME=task_tracker
      - DB_HOST=postgres
      - AMQP_URL=amqp://guest:guest@rabbitmq/
      - S3_ENDPOINT_INTERNAL=http://minio:9000
      - S3_ENDPOINT_PUBLIC=http://localhost:9000
      - S3_ACCESS_KEY=minioadmin
      - S3_SECRET_KEY=minioadmin
      - S3_BUCKET=attachments
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      migrations:
        condition: service_completed_successfully
      minio:
        condition: service_started

  minio:
    restart: unless-stopped
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

  rabbitmq:
    restart: unless-stopped
    ports:
      - 15672:15672
      - 5672:5672
    image:
      rabbitmq:3-management
    healthcheck:
      test: [ "CMD-SHELL", "rabbitmq-diagnostics -q ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    restart: unless-stopped
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5

  gateway:
    restart: unless-stopped
    build:
      context: .
      dockerfile: gateway/Dockerfile
    environment:
      - AMQP_URL=amqp://guest:guest@rabbitmq/
      - REDIS_URL=redis://redis:6379/0
      - METRICS_ENABLED=true
      # Yandex OAuth credentials come from the root .env (never committed)
      - YANDEX_CLIENT_ID=${YANDEX_CLIENT_ID:-}
      - YANDEX_CLIENT_SECRET=${YANDEX_CLIENT_SECRET:-}
      - YANDEX_REDIRECT_URI=${YANDEX_REDIRECT_URI:-http://localhost:8000/auth/yandex/callback}
      - YANDEX_OAUTH_BASE_URL=${YANDEX_OAUTH_BASE_URL:-https://oauth.yandex.ru}
      - YANDEX_USERINFO_URL=${YANDEX_USERINFO_URL:-https://login.yandex.ru/info}
      # Chat heartbeat knobs are overridable so e2e can use short timeouts
      - CHAT_HEARTBEAT_INTERVAL=${CHAT_HEARTBEAT_INTERVAL:-25}
      - CHAT_HEARTBEAT_TIMEOUT=${CHAT_HEARTBEAT_TIMEOUT:-60}
    ports:
      - "8000:8000"
    depends_on:
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      tasks:
        condition: service_started

  prometheus:
    restart: unless-stopped
    image: prom/prometheus:v2.54.1
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=7d"
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - promdata:/prometheus
    depends_on:
      gateway:
        condition: service_started

  grafana:
    restart: unless-stopped
    image: grafana/grafana:11.2.0
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    ports:
      - "3000:3000"
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafanadata:/var/lib/grafana
    depends_on:
      prometheus:
        condition: service_started

volumes:
  pgdata:
  miniodata:
  redisdata:
  promdata:
  grafanadata:


